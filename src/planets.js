function listPlanets() {
    let planets = ["Mercurio", "Venus", "Tierra", "Marte", "Jupiter"];
    for (let i = 0; i < planets.length; i++) {
        console.log(i,planets[i]);
    }
}
listPlanets();
